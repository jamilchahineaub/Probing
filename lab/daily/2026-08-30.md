### Stage 2

Started independent MATLAB/Simulink validation. The container has no MATLAB or
GNU Octave, so execution is blocked; independent `.m` physics, probe, tests,
environment recording, and Simulink builder were created without modifying
EXP-0001 through EXP-0007. Stage 3 remains unauthorized.

### Stage 2 completion

MATLAB R2026a Update 5 became available and the independent validation was
completed as `STAGE2_20260829T221139.099Z_s20101`. Seven tests passed. The
1,360-target population produced zero false-safe cases at all noise levels
(0.586% one-sided 95% upper bound), 88.38% nominal and 87.65% high-noise
accuracy, zero boundary false-safes, and zero probe violations. MATLAB,
Simulink, and post-hoc matched Python trajectories agreed to numerical
precision. Stage 2 passes; Stage 3 is authorized but not started.

### EXP-0008 — coupled 6-DoF UAV contact baseline

Froze Stage 1/2 before changing scientific code: all seven Stage 1 artifact
fingerprints matched, the then-current regression suite passed `81/81`, and the
passed Stage 2 run remained present. The baseline is commit
`95fcdbb5d08f2ffa46077aced7f5c882fb2627f8`, annotated tag
`stage2-complete-20260830`; the checkpoint-record commit is `9794684`.

Implemented explicit 6-DoF rigid-body dynamics with Hamilton quaternions, four
individual first-order rotor models, reaction torques and saturation, a
conventional cascaded controller, a rigid offset probe, unilateral contact, and
the frozen 1D target. The contact force and exact offset moment feed back into
the vehicle. The Stage 1/2 chirp, observation duration, feature definitions,
decision thresholds, and sustained-contact labels were not changed.

All 13 no-contact checks and nine contact-mechanics checks passed. The full
Python suite now passes `98/98`. A difficult lightly damped impact case converged
between `dt=1 ms` and `dt=0.5 ms`. The independent MATLAB equations and
subsystem-based Simulink checks passed and matched one representative coupled
trajectory to numerical precision.

The immutable run
`EXP-0008_20260830T123909.142216Z_s13101_f41ef89f` contains 107 physical
configurations and 321 frozen-policy evaluations. Median realized-force RMS
tracking error was `0.399 N` (`152.23%` relative), median bounded
cross-correlation lag was `224 ms`, and median faithful bandwidth was `2 Hz`;
48/107 configurations had no spectral bin within the predeclared magnitude and
phase tolerance. Every configuration separated and recontacted, and realized
force reached `4.994 N` despite the `0.5 N` reference. Attitude remained within
`2.893 deg`; no motor saturated and minimum reserve was `54.07%`.

On the nonduplicated 94-target primary population, nominal/high-noise binary
accuracy was `82.98%/81.91%`; false-safe rates were `11.76%/15.69%` with
one-sided 95% upper bounds `21.91%/26.53%`. Boundary nominal/high false-safe
rates were `53.33%/60.00%`. All 19 nominal false-safe configurations in the full
sweep violated only the settling criterion, and all changed physical
ground-truth outcome between the reduced and coupled sustained-contact tests.

Conclusion: the signal remains partially informative, but the frozen policy
does not transfer safely through this contact-delivery architecture. EXP-0008
is scientifically complete with decision `CONTINUE_STAGE_3A`. Stage 3B is not
authorized. The next justified experiment is a prospectively defined hybrid
contact-delivery controller focused on contact retention and impact suppression,
followed by an untuned retest of the frozen policy.
